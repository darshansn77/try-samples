# CI and PR triggers


jobs:
- job: test
  pool:
  name: MY-WORK
  vmImage: 'darshan-wsl'
    
  steps:
   - task: UseDotNet@2
     displayName: 'Use .NET Core sdk'
     inputs:
       packageType: sdk
       version: 3.1.200
       installationPath: $(Agent.ToolsDirectory)/dotnet
   - task: UseDotNet@2
     displayName: 'Use .NET Core sdk'
     inputs:
       packageType: sdk
       version: 2.1.700
       installationPath: $(Agent.ToolsDirectory)/dotnet
   - script: build.cmd
   
